# Архитектура

[English](../en/ARCHITECTURE.md) | Русский

## Что это за система

Обмен сообщениями через цепочку из трёх узлов-ретрансляторов с вложенным шифрованием маршрута.
Ни один узел не знает одновременно отправителя и получателя, ключевой материал существует только
в оперативной памяти узла, размер ячейки постоянный.

## Компоненты

| Компонент | Назначение |
|---|---|
| client | строит цепочку, шифрует слои, шлёт полезные и фоновые ячейки, принимает ответ |
| relay | снимает свой слой, пересылает следующему узлу; на диск не пишет ничего |
| crypto | CryptoProvider: два набора примитивов за одним интерфейсом |
| crypto/secmem | буферы ключей вне кучи Go: mlock, запрет дампов, зануление |
| wire | формат ячейки и вложенное шифрование маршрута |
| vault | контейнер клиента с двумя независимыми томами: пароль-легенда и настоящий |
| lab | прогон экспериментов, наблюдатель, метрики, отчёты в artifacts/ |
| web | дашборд стенда: граф сети, что видит каждый узел, результат атаки в реальном времени |

## Поток сообщения

```
client-a -> relay-1 -> relay-2 -> relay-3 -> client-b
```

1. Клиент выбирает цепочку из трёх узлов по списку узлов с их долговременными ключами.
2. С каждым узлом отдельно согласуется эфемерный ключ сессии. Узел подтверждает свою личность
   подписью, клиент проверяет её по сертификату лабораторного УЦ.
3. Клиент шифрует сообщение тремя слоями: внешний для relay-1, внутренний для relay-3.
4. Каждый узел снимает ровно свой слой и узнаёт только адрес следующего хопа.
5. Ключи сессии живут до конца сессии и зануляются.

## Формат ячейки

Ячейка постоянного размера 512 байт, формат одинаков для полезных и фоновых ячеек.

| Поле | Размер | Назначение |
|---|---|---|
| version | 1 | версия формата |
| kind | 1 | полезная, фоновая, служебная |
| circuit | 8 | идентификатор цепочки, свой на каждом канале между соседями |
| counter | 8 | номер ячейки в цепочке, служит источником nonce и защитой от повтора |
| body | 494 | слои: три тега аутентификации по 16 байт, длина и полезная нагрузка |

- Nonce не передаётся: он выводится из направления, идентификатора цепочки и counter. Ключ
  принадлежит одному узлу одной цепочки, поэтому пара (направление, counter) под ним не повторяется.
- Каждый слой это AEAD над содержимым следующего слоя. Ассоциированные данные включают версию,
  вид ячейки, counter и номер узла в цепочке, поэтому ячейку нельзя подставить на чужую позицию.
- Идентификатор цепочки в ассоциированные данные не входит: он переписывается на каждом канале.
- Слой узла с номером i занимает первые 494 - 16 * i байт тела. Сняв свой слой, узел дополняет
  тело случайными байтами до 494, поэтому на каждом канале ячейка одного размера и позиция узла
  в цепочке по размеру не видна.
- Полезная нагрузка внутри последнего слоя: два байта длины, данные, случайное заполнение.
  При трёх узлах на сообщение остаётся 444 байта.
- Узел ведёт окно принятых counter и отбрасывает повтор: пропущенный повтор дал бы активному
  наблюдателю бесплатную временную метку.

## Канальное шифрование

Ячейка между соседями идёт не открыто, а внутри зашифрованного кадра. Без этого слоя заголовок
ячейки виден на проводе, а счётчик в нём одинаков на всех каналах цепочки: наблюдатель на входе
и на выходе связал бы поток по совпадению номеров, без всякой статистики.

- Рукопожатие: инициатор присылает эфемерный открытый ключ, ответчик свой. Общий секрет из двух
  эфемерных ключей даёт прямую секретность канала.
- Клиент знает долговременный ключ входного узла и подмешивает его в секрет: канал до входа
  аутентифицирован. Между узлами канал анонимный: он скрывает заголовки от пассивного наблюдателя,
  а привязку содержимого к узлам, выбранным клиентом, обеспечивают слои.
- Из секрета выводятся два ключа, по одному на направление. Nonce это номер кадра в направлении.
- Кадр это ячейка 512 байт плюс тег 16 байт, итого 528. На проводе после рукопожатия только кадры
  одного размера, ни идентификаторов, ни счётчиков.
- Все примитивы канального слоя берутся из того же CryptoProvider, поэтому канал работает и на
  наборе ГОСТ.

## Режимы отправки

| Режим | Как идут ячейки | Что видно наблюдателю |
|---|---|---|
| Немедленный | ячейка уходит, как только есть что отправить, фоновые добавляются сверху | рисунок отправок повторяет разговор |
| Постоянная скорость | ячейки уходят по расписанию, полезная занимает место фоновой | рисунок на канале не зависит от разговора |

Второй режим и есть мера против связывания потоков. Цена: сообщение ждёт своего слота, поэтому
задержка растёт при низкой скорости расписания и падает при высокой.

## Обратный канал

Ответ идёт по той же цепочке в обратную сторону: выходной узел накладывает свой слой, каждый
следующий узел добавляет свой, и только клиент снимает их все. Направление входит в nonce, поэтому
прямые и обратные ячейки под одним ключом никогда не совпадают по nonce. Счётчик обратного
направления свой, у каждого узла своё окно защиты от повтора для каждой стороны.

## Установка цепочки

Установка идёт одной служебной ячейкой того же размера 512 байт, без дополнительных обменов.

- Клиент знает адреса узлов и их долговременные открытые ключи.
- Для каждого узла клиент генерирует эфемерную пару и согласует общий секрет с долговременным
  ключом узла. Привязка к сессии идёт через идентификатор канала в этот узел.
- Из общего секрета выводятся два ключа: для служебной ячейки и для ячеек данных.
- Служебная ячейка вложена так же, как ячейка данных: слой каждого узла содержит его эфемерный
  открытый ключ, адрес следующего узла, идентификатор следующего канала и слой для следующего узла.
- Номер узла в цепочке едет в поле counter служебной ячейки: узел должен знать свою позицию
  раньше, чем сможет определить длину своего слоя.
- Сняв свой слой, узел дополняет ячейку случайными байтами до 512 и пересылает дальше.

Пустой адрес следующего узла означает, что это выходной узел.

Разрыв цепочки:

- Цепочка привязана к каналу, по которому пришла её служебная ячейка. Ячейка с тем же
  идентификатором из другого канала отбрасывается, повторная служебная ячейка с занятым
  идентификатором тоже.
- Закрытие канала в любом звене закрывает соседние каналы этой цепочки в обе стороны, разрыв
  доходит до клиента и до выходного узла.
- Ключи цепочки освобождаются после остановки всех горутин, которые ими пользуются.
- Клиент видит разрыв как закрытие канала ответов и завершается. Оркестратор поднимает его заново
  на новой цепочке.

## Аутентификация узлов

- У каждого узла долговременная пара подписи и сертификат лабораторного УЦ.
- УЦ поднимает lab, он живёт вне кластера и нужен только для стенда.
- Клиент проверяет подпись узла при согласовании ключа. Компрометация УЦ не даёт доступа к
  содержимому прошлых сессий: слои шифруются эфемерными ключами, цепочку выбирает клиент.

## Что снимается при измерениях

| Источник | Данные |
|---|---|
| client | время отправки и приёма каждой ячейки, потери, идентификатор потока |
| relay | агрегированные счётчики в stdout: принято, переслано, отброшено. Без идентификаторов потоков |
| сеть | записи трафика на входе и выходе цепочки для атаки корреляции |
| память | дампы процесса relay в сценарии извлечения ключей |

Всё пишется в artifacts/ вместе с конфигурацией прогона и зерном генератора.

## Раскладка пакетов

| Пакет | Назначение | Зависит от |
|---|---|---|
| crypto | интерфейс CryptoProvider | crypto/secmem |
| crypto/gost, crypto/c25519 | наборы примитивов | crypto, secmem, внешние библиотеки |
| crypto/secmem | память под ключи | x/sys/unix |
| crypto/providertest | тесты соответствия контракту | crypto |
| wire | формат ячейки, слои, окно повторов | crypto |
| relay | узел-ретранслятор | crypto, wire |
| client | отправка, приём, фоновый трафик | crypto, wire |
| vault | контейнер с двумя томами | crypto, crypto/secmem |
| lab/* | сценарии, наблюдатель, метрики, отчёты | client, relay, wire |
| web | дашборд стенда | lab |
| cmd/* | точки входа и конфигурация | пакеты выше |

Правило: relay, client и wire ничего не знают о lab. Экспериментальная обвязка зависит от системы,
не наоборот.

## Контейнер клиента

Узлы не хранят ничего, клиент хранит историю переписки. Контейнер держит два независимых тома:
один открывается паролем-легендой, другой настоящим паролем. Ключ тома выводится из пароля через
Argon2id, в файле нет заголовка, по которому видно число томов, а неиспользованное пространство
заполнено случайными данными.

Свойство проверяется, а не декларируется: энтропия и набор тестов NIST STS показывают, что файл
неотличим от случайных данных. Границы свойства (следы на уровне файловой системы и накопителя,
наблюдатель с несколькими снимками) вынесены в LIMITATIONS и проговариваются явно.

## Развёртывание

- deploy/compose: docker-compose на три relay и двух клиентов, основной режим разработки.
- deploy/kind и deploy/base: тот же состав в кластере Kubernetes, для демонстрации на защите.
  Манифесты не содержат Secret с ключами, тома только tmpfs в оперативной памяти,
  корневая файловая система только для чтения.
