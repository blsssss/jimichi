// Package lab runs the experiments: it drives clients and relays through a
// scenario, records what the attacker learned and what it cost, and writes the
// result with the configuration that produced it.
package lab
