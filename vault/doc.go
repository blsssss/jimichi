// Package vault is the client-side container with two independent volumes: one
// password opens the decoy, another opens the real history. Ciphertext carries
// no header that would reveal how many volumes exist.
package vault
